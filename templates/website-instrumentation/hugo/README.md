# Hugo Website Instrumentation

Create a partial that reads page params generated by the Markdown Website frontmatter binding.
The partial should render SMM meta markers, the JSON config block, `/instrumentation/smm-analytics.js`, providerbridge script includes, and CTA partial attributes.
Do not pass arbitrary user template expressions into event properties.
