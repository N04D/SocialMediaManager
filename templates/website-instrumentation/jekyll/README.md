# Jekyll Website Instrumentation

Add an include that reads safe frontmatter fields under `analytics`.
The include renders public markers, `/instrumentation/smm-analytics.js`, the Plausible bridge, and data attributes for CTAs.
Liquid content is not used as analytics event data.
