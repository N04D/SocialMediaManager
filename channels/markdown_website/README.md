# Markdown Website Channel

`channel.markdown_website` is a first-party owned-publication channel. It renders immutable full-article snapshots to deterministic Markdown, writes only allowlisted paths in a managed Git worktree, commits exact mutation paths, optionally pushes to an allowlisted remote branch, and verifies the public URL before dependent social targets are released.
