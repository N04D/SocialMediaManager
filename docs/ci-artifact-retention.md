# CI Artifact Retention

Default policy:

- downloaded provider archives are short-lived or not retained;
- normalized evidence packages follow evidence retention;
- import attestations and audit history are retained;
- the last verified package is protected;
- legal hold prevents deletion.

Retention never deletes immutable certification evidence or attestations needed
for readiness history.
