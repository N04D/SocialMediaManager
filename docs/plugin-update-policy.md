# Plugin Update Policy

Updates are never automatic. The operator refreshes the registry, reviews changelog, permission delta, capability delta, signer identity, compatibility, downloads explicitly, verifies, installs side by side as disabled, then separately activates after restart. Expanded permissions require additional confirmation.
