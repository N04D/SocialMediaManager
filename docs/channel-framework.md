# Channel Framework

Channel metadata may declare `social_distribution`, `owned_publication`, `newsletter`, `community`, or `commerce`. LinkedIn and Mastodon remain `social_distribution`; `channel.markdown_website` is `owned_publication`.

The family label supports UI grouping, funnel views, analytics, and dependency rules without changing existing runtime semantics.
