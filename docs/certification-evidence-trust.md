# Certification Evidence Trust

Trust levels remain explicit: unsigned local, signed local, verified CI
artifact, verified staging provider, invalid, untrusted, stale and revoked.

Phase 29 adds host signer health and import attestations. A host-signed import
attestation says this host verified one concrete artifact; it is not a global
supply-chain attestation.
