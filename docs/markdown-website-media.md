# Markdown Website Media

Media is resolved through the existing Media Framework and materialized before writing. Website filenames are deterministic and checksum-bound, using asset and variant identifiers plus a safe name.

The plugin does not read arbitrary local media paths and does not remove shared media during unpublish.
