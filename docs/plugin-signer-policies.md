# Plugin Signer Policies

Signer policies bind a plugin id to exact certificate identities, OIDC issuers, optional repository/workflow identities, minimum signatures, transparency log requirements, signed timestamp requirements, validity window, and revoked identities. Official plugins must not use broad wildcard identities. Signer changes require registry review.
