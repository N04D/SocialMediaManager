# Alpha Demo Mode

Deterministic demo mode uses temporary SQLite, temporary vault and repository paths, fake Markdown Website, fake Mastodon, fake LinkedIn, fake Plausible, and synthetic content. It performs no external network calls, uses no real secrets, does not read `content/` or `drafts/`, and makes no live provider, alpha-production, or production-readiness claim.
