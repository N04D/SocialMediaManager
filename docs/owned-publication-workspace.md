

## Phase 32 alpha onboarding

The workspace can now be created or reused from alpha onboarding. Completion stores resource bindings instead of relying on browser state, and first content is converted into an immutable revision before publication review.
