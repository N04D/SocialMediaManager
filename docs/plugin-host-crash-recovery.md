# Plugin Host Crash Recovery

Crashes are classified as startup failure, handshake failure, protocol violation, uncaught exception, process exit, signal termination, resource limit, watchdog termination, operator stop, environment failure, or identity failure. Post-mutation crashes become uncertain or require remote verification; the plugin host never performs a full publish retry.
