# Plugin Registry TUF

The registry uses TUF roles: root, targets, snapshot, and timestamp. The client validates trusted root, expiry, rollback, target hashes, target sizes, snapshot/targets consistency, and release status before download. The local fixture uses test-only metadata and is not production key guidance.
