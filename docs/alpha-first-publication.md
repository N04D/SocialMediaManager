# Alpha First Publication

The first publication flow creates an immutable content revision, channel variant bindings, publication snapshots, a mutation summary, and a final review. Publication requires the exact confirmation text: `Publish this immutable revision using this plan`. Onboarding coordinates existing execution services and does not write Git, call channel APIs, run browser automation, retry mutations, or produce evidence itself.
