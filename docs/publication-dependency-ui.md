# Publication Dependency UI

The dependency view shows predecessor target, required state, dependent target, timeout policy, failure policy, and current unlock status.

Cycles and self-dependencies are blocked. A dependency timeout never bypasses website verification to publish a social post with an unverified URL.
