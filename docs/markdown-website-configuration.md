# Markdown Website Configuration

Accounts use `MarkdownWebsiteAccountConfig` with branch, content root, media root, public URL template, frontmatter profile, push policy, and verification policy.

Push policies are `commit_only` and `commit_and_push`. Verification policies are `commit_only`, `remote_commit`, and `public_url`. Social posts that link to the website must depend on `publication_verified`.
