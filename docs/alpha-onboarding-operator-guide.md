# Alpha Onboarding Operator Guide

Start with `plugin-sdk onboarding start` or `plugin-sdk onboarding demo-start`. The wizard stores every step durably in SQLite and can resume after reload or restart. Website publishing is the minimum required alpha destination. Analytics, instrumentation, Mastodon, LinkedIn, host signer, and GitHub CI evidence are setup checklist items unless selected policies make them required.
