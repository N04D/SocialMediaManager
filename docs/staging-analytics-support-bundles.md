# Staging Analytics Support Bundles

The instrumentation support bundle may include framework versions, profile IDs,
manifest checksums, report checksums, safe event names, safe property names,
quality status, and staging run summaries.

It must not include event payload values, full URLs with query parameters, raw
provider responses, secrets, tokens, cookies, browser profiles, screenshots
with non-synthetic content, production content, or a database file.
