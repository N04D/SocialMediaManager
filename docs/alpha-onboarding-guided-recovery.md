# Alpha Guided Recovery

Blocking findings expose safe actions such as open settings, run doctor, retry read-only check, create a missing managed reference, select another registered resource, or show an operator runbook. The wizard never changes credentials, replaces databases, force-runs Git, repeats uncertain external mutations, or marks sandbox certification as passed.
