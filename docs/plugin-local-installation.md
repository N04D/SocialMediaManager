# Plugin Local Installation

Download, verification, installation, and activation are separate actions. Installation uses a verified local cache, no index, no dependency download, no build, no editable mode, no user-site or system-site changes. New installs are `installed_disabled`. Local unsigned installs require developer mode and `--developer-unsigned`, remain experimental, and still require separate activation.
